# Test environment configuration
# These values are used during testing

# OpenAI Configuration (use test key or mock)
OPENAI_API_KEY=sk-test-key-for-testing-only
GPT5_DEFAULT_MODEL=gpt-5-nano
OPENAI_TIMEOUT=30000

# Server Configuration
SERVER_NAME=gpt5-claude-mcp-test
MAX_RETRIES=2
RETRY_DELAY=500

# Logging Configuration (minimal logging during tests)
LOG_LEVEL=error
LOG_REQUESTS=false
LOG_RESPONSES=false